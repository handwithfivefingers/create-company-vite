version: '3'
services:
  cc-service:
    build: .
    ports:
      - 3000:3000
      - 3001:3001
    volumes:
      - .:/app
    networks:
      - app_network
networks:
  app_network:
    driver: bridge
