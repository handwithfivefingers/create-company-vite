version: '3'
services:
  cc-service:
    build: .
    ports:
      - 3000:3000
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - app_network
networks:
  app_network:
    driver: bridge
